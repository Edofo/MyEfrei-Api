type User {
    uuid: ID!
    name: String!
    email: String!
}

input RegisterInput {
    name: String!
    email: String!
    password: String!
}

type AccessToken {
    accessToken: String!
}

type Mutation {
    register(data: RegisterInput!): User!
    login(email: String!, password: String!): AccessToken!
}
